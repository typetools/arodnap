--- /tmp/orig-4657407824699181471.java	2025-09-24 22:08:06.489950512 +0000
+++ /home/smala009/RLF/cf-rlc/june2020_dataset_NJR/url70ed135da7_ralfbiedert_jcores_tgz-pJ8-sandbox_SimpleFilterTestJ8/src/sandbox/SimpleUnzip.java	2025-09-24 22:08:03.005706952 +0000
@@ -118,17 +118,17 @@
                     int currentByte;
                     // establish buffer for writing file
                     byte data[] = new byte[BUFFER];
-
-                    // write the current file to disk
-                    final FileOutputStream fos = new FileOutputStream(destFile);
-                    final BufferedOutputStream dest = new BufferedOutputStream(fos, BUFFER);
-
-                    // read and write until last byte is encountered
-                    while ((currentByte = is.read(data, 0, BUFFER)) != -1) {
+                    try (final FileOutputStream fos = new FileOutputStream(destFile)) {
+                        final BufferedOutputStream dest = new BufferedOutputStream(fos, BUFFER);
+                        while ((currentByte = is.read(data, 0, BUFFER)) != -1) {
                         dest.write(data, 0, currentByte);
                     }
-                    dest.flush();
-                    dest.close();
+                        dest.flush();
+                    }
+
+
+
+
                     is.close();
                 }
             } catch (IOException ioe) {
