--- /tmp/orig-6081867806962721031.java	2025-09-24 22:15:12.403723351 +0000
+++ /home/smala009/RLF/cf-rlc/june2020_dataset_NJR/url7a45f192ca_mfullen_ping_pong_net_tgz-pJ8-ping_pong_net_client_io_TestClientNonPPNServerJ8/src/ping/pong/net/server/io/IoServerTest.java	2025-09-24 22:15:10.347579620 +0000
@@ -240,11 +240,12 @@
     public void testAddMessageListener()
     {
         MessageListener<? super Connection, Envelope> listener = null;
-        IoServer instance = new IoServer();
-        instance.addMessageListener(listener);
-        assertEquals(0, instance.messageListeners.size());
-
-        listener = new MessageListener<Connection, Envelope>()
+        ping.pong.net.server.io.IoServer instance = null;
+        try {
+            instance = new IoServer();
+            instance.addMessageListener(listener);
+            assertEquals(0, instance.messageListeners.size());
+            listener = new MessageListener<Connection, Envelope>()
         {
             @Override
             public void messageReceived(Connection source, Envelope message)
@@ -253,9 +254,19 @@
                 assertNotNull(message);
             }
         };
+            instance.addMessageListener(listener);
+            assertEquals(1, instance.messageListeners.size());
+        } finally {
+            if (instance != null) {
+                try {
+                    instance.shutdown();
+                } catch (Exception e) {
+                    e.printStackTrace();
+                }
+            }
+        }
+
 
-        instance.addMessageListener(listener);
-        assertEquals(1, instance.messageListeners.size());
     }
 
     /**
