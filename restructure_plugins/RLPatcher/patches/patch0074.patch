--- /tmp/orig-9796751235004715995.java	2025-09-24 21:39:14.384878755 +0000
+++ /home/smala009/RLF/cf-rlc/june2020_dataset_NJR/url0e7d57473a_kyorohiro_HetimaUtil_tgz-pJ8-net_hetimatan_net_http_HttpServer3xxJ8/src/net/hetimatan/io/file/TestForKyoroFileForFiles.java	2025-09-24 21:39:12.288732285 +0000
@@ -34,22 +34,33 @@
 		}
 		CashKyoroFile[] files = new CashKyoroFile[1];
 		files[0] = new CashKyoroFile(testdata);
-		KyoroFileForFiles kff = new KyoroFileForFiles(files);
-		byte[] buffer = new byte[256];
-		int len = kff.read(buffer);
-		assertEquals(256, len);
-		for(int i=0;i<256;i++) {
+		net.hetimatan.io.file.KyoroFileForFiles kff = null;
+		try {
+		    kff = new KyoroFileForFiles(files);
+		    byte[] buffer = new byte[256];
+		    int len = kff.read(buffer);
+		    assertEquals(256, len);
+		    for(int i=0;i<256;i++) {
 			assertEquals(testdata[i], buffer[i]);
 		}
-
-		
-		buffer = new byte[testdata.length];
-		kff.seek(0);
-		len = kff.read(buffer);
-		assertEquals(buffer.length, len);
-		for(int i=0;i<buffer.length;i++) {
+		    buffer = new byte[testdata.length];
+		    kff.seek(0);
+		    len = kff.read(buffer);
+		    assertEquals(buffer.length, len);
+		    for(int i=0;i<buffer.length;i++) {
 			assertEquals(testdata[i], buffer[i]);
 		}
+		} finally {
+		    if (kff != null) {
+		        try {
+		            kff.close();
+		        } catch (Exception e) {
+		            e.printStackTrace();
+		        }
+		    }
+		}
+
+		
 	}
 
 	public void testTwo001() throws IOException {
