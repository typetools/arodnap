--- /tmp/orig-8062356693339388294.java	2025-09-24 22:24:33.766964711 +0000
+++ /home/smala009/RLF/cf-rlc/june2020_dataset_NJR/url80e39b1957_tobikster_IJN_headers_recognition_tgz-pJ8-org_maltparser_examples_CreatePhraseStructureGraphJ8/src/org/maltparser/parser/guide/instance/FeatureDivideModel.java	2025-09-24 22:24:30.154712199 +0000
@@ -287,8 +287,10 @@
 	 * @throws MaltChainedException
 	 */
 	protected void load() throws MaltChainedException {
-		try {
-			final BufferedReader in = new BufferedReader(getGuide().getConfiguration().getConfigurationDir().getInputStreamReaderFromConfigFile(getModelName()+".dsm"));
+	org.maltparser.core.config.ConfigurationDir __arodnap_temp0 = null;
+	try {
+	__arodnap_temp0 = getGuide().getConfiguration().getConfigurationDir();
+		final BufferedReader in = new BufferedReader(__arodnap_temp0.getInputStreamReaderFromConfigFile(getModelName()+".dsm"));
 			final Pattern tabPattern = Pattern.compile("\t");
 			while(true) {
 				String line = in.readLine();
@@ -318,6 +320,14 @@
 		} catch (IOException e) {
 			throw new GuideException("Could not read from the guide model settings file '"+getModelName()+".dsm"+"', when " +
 					"loading the guide model settings. ", e);
+		} finally {
+		    if (__arodnap_temp0 != null) {
+		        try {
+		            __arodnap_temp0.terminate();
+		        } catch (Exception e) {
+		            e.printStackTrace();
+		        }
+		    }
 		}	
 	}
 	
