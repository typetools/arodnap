# RLFixer example programs

Small programs, one directory per fix strategy (loops, parameters, returns, throws,
try/catch) plus parsing and resource-classification cases, from RLFixer's development.
`test-projects/gradle-pipeline-coverage` includes the strategy ones, so the end-to-end
tests run RLFixer on them.
