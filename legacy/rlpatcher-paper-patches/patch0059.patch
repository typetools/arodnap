--- /tmp/orig-8185657947767893618.java	2025-09-24 21:37:43.094499778 +0000
+++ /home/smala009/RLF/cf-rlc/june2020_dataset_NJR/url0e7d57473a_kyorohiro_HetimaUtil_tgz-pJ8-net_hetimatan_net_http_HttpServer3xxJ8/src/net/hetimatan/util/http/TestForHttpHeader.java	2025-09-24 21:37:40.942349394 +0000
@@ -13,15 +13,27 @@
 	}
 
 	public void testEncode001() throws IOException {
-		HttpRequestHeader header = new HttpRequestHeader("key","value");
-		CashKyoroFile output = new CashKyoroFile(512);
-		header.encode(output.getLastOutput());
+	net.hetimatan.util.http.HttpRequestHeader header = null;
+	try {
+	    header = new HttpRequestHeader("key","value");
+	    CashKyoroFile output = new CashKyoroFile(512);
+	    header.encode(output.getLastOutput());
+	    output.seek(0);
+	    byte[] buffer = new byte[(int)output.length()];
+	    int len = output.read(buffer);
+	    String tag = new String(buffer, 0, len);
+	    assertEquals("key: value\r\n", tag);
+	} finally {
+	    if (header != null) {
+	        try {
+	            header.createEncode(null);
+	        } catch (Exception e) {
+	            e.printStackTrace();
+	        }
+	    }
+	}
+
 
-		output.seek(0);
-		byte[] buffer = new byte[(int)output.length()];
-		int len = output.read(buffer);
-		String tag = new String(buffer, 0, len);
-		assertEquals("key: value\r\n", tag);
 	}
 
 	public void testDecode001() throws IOException {
